import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

const SingleSlice = createSlice({
  name: 'single',
  initialState,
  reducers: {},
});

export const {} = SingleSlice.actions'single'

export default SingleSlice.reducer'single'
