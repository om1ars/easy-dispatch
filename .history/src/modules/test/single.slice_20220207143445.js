import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

const single.slice = createSlice({
  name: second,
  initialState,
  reducers: {},
});

export const {} = single.slice.actions;

export default single.slice.reducer;
