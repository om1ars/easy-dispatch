import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

const openSidebar.slice = createSlice({
  name: second,
  initialState,
  reducers: {},
});

export const {} = openSidebar.slice.actions;

export default openSidebar.slice.reducer;
