import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

const test.slice = createSlice({
  name: second,
  initialState,
  reducers: {},
});

export const {} = test.slice.actions;

export default test.slice.reducer;
